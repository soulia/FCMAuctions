@model IEnumerable<BidListViewModel>

@{
    ViewBag.Title = "Index";
}

<div class="child-column">
    <h1>@Html.DisplayFor(modelItem => Model.FirstOrDefault().ItemName)</h1>
    <h2>Value: @Html.DisplayFor(modelItem => Model.FirstOrDefault().Value)</h2>
    <h3>
        @Html.DisplayFor(modelItem => Model.FirstOrDefault().Description)
    </h3>
    <hr/>
</div>

<div class="child-column">
    <ul class="child-row">
        <li><b>@Html.DisplayNameFor(model => model.UserName)</b></li>
        <li><b>Phone</b></li>
        <li><b>@Html.DisplayNameFor(model => model.Bid)</b></li>
    </ul>
</div>
@if (Model.Any())
{
    //foreach (var item in Model)
    //{
    <div class="child-column">
        <ul class="child-row">
                <li>@Html.DisplayFor(modelItem => Model.FirstOrDefault().UserName)</li>
            <li></li>
                <li>@Html.DisplayFor(modelItem => Model.FirstOrDefault().Bid)</li>
        </ul>
    </div>
    //}
}

@for (int i = 0; i <= 23; i++)
{
    <hr />
}