@model IEnumerable<FCMAuction.Models.ItemBid>

<table>
    <tr>
        <th>
            Bids
        </th>
        <th>
            Id
        </th>
        <th></th>
    </tr>
    @if(Model.Any())
    { 
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemId)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.Id })*@ 
            </td>
        </tr>
    }
    }

</table>

